// errors.go contains sentinel errors exposed by the sync package.
package sync

// Package-level sentinel errors live in errors.go.
// Add errors here when callers need to distinguish them with errors.Is.
