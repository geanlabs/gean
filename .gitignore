# Build artifacts
bin/
/gean

# Runtime data
data/

# Local testnet config (generated by keygen). These directories hold validator
# and node secret keys, so match any suffixed variant rather than one name.
testnet/
testnet-*/

# Shadow simulator artifacts (generated by shadow/gen_shadow_yaml.sh)
shadow/shadow.yaml
shadow/net/
shadow/shadow.data/

# Spec fixtures (cloned separately)
leanSpec/

# Lean-quickstart (cloned separately)
lean-quickstart/

# Logs
*.log
output.log
devnet.log

# Rust build artifacts
xmss/rust/target/
